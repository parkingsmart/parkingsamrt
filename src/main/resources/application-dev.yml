cors:
  allowed-origins: "*"
  allowed-methods: GET, PUT, POST, DELETE, PATCH
  allowed-headers: origin, content-type, accept, authorization
  exposed-headers:
  allow-credentials: true
  max-age: 1800
server:
  port: 8080
logging:
  level:
    org:
      springframework:
        web: DEBUG
      hibernate: DEBUG
spring:
  profiles: dev
  datasource:
    url: jdbc:mysql://localhost/parkingsmart-backend
    username: root
    password: oocl
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update


